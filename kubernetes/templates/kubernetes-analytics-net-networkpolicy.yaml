apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: kubernetes-analytics-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/kubernetes-analytics-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/kubernetes-analytics-net: "true"
